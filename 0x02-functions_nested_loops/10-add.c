#include "main.h"
#include <stdio.h>

/**
 * add - add two numbers
 *
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */

int add(int x, int y)
{
	return (x+y);
}
